---
import GitHubIcon from '../icons/GitHub.astro';
import CodeIcon from '../icons/Code.astro';
import { type ui } from '../i18n/ui';

interface Props {
  lang: keyof typeof ui;
}

const { lang } = Astro.props;

const TAGS = {
  AWS: {
    name: "AWS",
    class: "bg-[#232F3E] text-white",
  },
  TERRAFORM: {
    name: "Terraform",
    class: "bg-[#844FBA] text-white",
  },
  GITLAB: {
    name: "GitLab CI/CD",
    class: "bg-[#FC6D26] text-white",
  },
  AZURE: {
    name: "Azure DevOps",
    class: "bg-[#0078D4] text-white",
  },
  PYTHON: {
    name: "Python / Bash",
    class: "bg-[#3776AB] text-white",
  },
  AI: {
    name: "AI / LLMs",
    class: "bg-[#000000] text-white",
  },
  ASTRO: {
    name: "Astro",
    class: "bg-[#FF5D01] text-white",
  },
  TAILWIND: {
    name: "Tailwind CSS",
    class: "bg-[#06B6D4] text-white",
  }
}

const PROJECTS = [
  {
    title: "G.A.I.A Framework",
    description: lang === 'es' ? "AI-Driven Platform Engineering & DevOps Swarm: Un enjambre de agentes autónomos inspirado en Horizon Zero Dawn. Diseñado para convertir GitHub Copilot y LLMs (Claude 3, modelos locales de HuggingFace) en un motor determinista para la Orquestación de LLM. Cubre todo el ciclo de vida del software: desde la forja de infraestructura autónoma (Terraform) hasta la observabilidad avanzada." : "AI-Driven Platform Engineering & DevOps Swarm: An autonomous agent swarm inspired by Horizon Zero Dawn. Designed to turn GitHub Copilot and LLMs (Claude 3, local HuggingFace models) into a deterministic engine for LLM Orchestration. Covers the entire software lifecycle: from autonomous infrastructure forging (Terraform) to advanced observability.",
    link: "",
    github: "https://github.com/l3onkers/gaia-autonomous-framework",
    image: "/projects/gaia.jpg",
    tags: [TAGS.AI, TAGS.TERRAFORM, TAGS.PYTHON],
  },
  {
    title: "Personal Portfolio",
    description: lang === 'es' ? "Desarrollo de portfolio personal web enfocado en el rendimiento y accesibilidad. Construido con Astro y Tailwind CSS, desplegado utilizando GitHub Pages." : "Development of a personal web portfolio focused on performance and accessibility. Built with Astro and Tailwind CSS, deployed using GitHub Pages.",
    link: "https://l3onkers.github.io/",
    github: "https://github.com/l3onkers/l3onkers.github.io",
    image: "https://images.unsplash.com/photo-1507238691740-187a5b1d37b8?auto=format&fit=crop&q=80&w=1000",
    tags: [TAGS.ASTRO, TAGS.TAILWIND],
  }
]
---

<div class="flex flex-col gap-y-16">
  {
    PROJECTS.map(({ image, title, description, tags, link, github }, i) => (
      <article class="anim-fade-in flex flex-col space-x-0 space-y-8 group md:flex-row md:space-x-8 md:space-y-0" style={`animation-delay: ${i * 150}ms`}>
        <div class="w-full md:w-1/2">
          <div class="relative flex flex-col items-center col-span-6 row-span-5 gap-8 transition-all duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl sm:rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-[0_20px_60px_-10px_rgba(139,92,246,0.2)] lg:border lg:border-gray-200 dark:lg:border-gray-800 lg:hover:border-violet-400/30 dark:lg:hover:bg-gray-800/50">
            <img alt={title} class="object-cover object-top w-full h-56 transition duration-500 sm:h-full md:scale-110 md:group-hover:scale-105" src={image} loading="lazy" />
            <div class="absolute inset-0 bg-gradient-to-t from-black/40 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none" aria-hidden="true"></div>
          </div>
        </div>

        <div class="w-full md:w-1/2 md:max-w-lg">
          <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">
            {title}
          </h3>
          <div class="flex flex-wrap mt-2">
            <ul class="flex flex-row mb-2 gap-x-2">
              {tags.map((tag) => (
                <li>
                  <span class={`flex gap-x-2 rounded-full text-xs py-1 px-2 border border-black/10 dark:border-white/10 ${tag.class}`}>
                    {tag.name}
                  </span>
                </li>
              ))}
            </ul>

            <div class="mt-2 text-gray-700 dark:text-gray-400">{description}</div>
            <footer class="flex items-end justify-start mt-4 gap-x-4">
              {github && (
                <a href={github} aria-label={`Ver código fuente del proyecto ${title}`} class="bg-gray-200 dark:bg-gray-800 hover:bg-gray-300 dark:hover:bg-gray-700 text-gray-900 dark:text-white py-2 px-4 border border-gray-300 dark:border-gray-600 rounded-full inline-flex items-center gap-x-2 transition">
                  <GitHubIcon class="size-4" />
                  Code
                </a>
              )}
              {link && (
                <a href={link} aria-label={`Ver demostración en vivo del proyecto ${title}`} class="bg-violet-600 hover:bg-violet-700 text-white py-2 px-4 border border-violet-600 rounded-full inline-flex items-center gap-x-2 transition">
                  <CodeIcon class="size-4" />
                  Preview
                </a>
              )}
            </footer>
          </div>
        </div>
      </article>
    ))
  }
</div>
